# Copy to .env for local demos. Do not commit the real .env file.
# Qwen/DashScope OpenAI-compatible endpoint is configurable so the demo can move between machines.
DASHSCOPE_API_KEY=
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen3-max
